# D005 2026-09-22 周二

## 今日目标（昨天定的，1-3 条）

1.进行解码循环的学习
2.将解码获取的原始数据进行像素格式转换

## 产出（必须至少有一条）

- 完成了解码循环，学习了解码循环的整体流程，创建格式上下文，创建解码上下文，创建解码器，与解码上下文进行链接，创建AVPacket获取压缩数据，AVFrame用来接收解码后的数据，用循环，识别视频流，并解码取帧，用avcodec_send_packet把获取的包输入给解码上下文进行解码器解码，然后avcodec_receive_frame把解码后的数据传给frame，进行解码取出第1帧

- 完成了用sws_scale进行像素格式转换，因为直接取出来的帧是YUV420P格式，并非是图像格式，需要进行一次转换，就用sws_scale转换，PPM是简单图像格式，以RGB24格式排列，要用到swscontext创建格式转换上下文并进行转换，rgb[4]是平面大小数据，而rgblinesize是指一行的数据，开辟内存空间的时候使用av_image_alloc进行空间开辟，它会进行内存对齐，因为一行的数据并非是w*3，直接用w\*3的话会有问题。还有就是我们进行的写入文件操作，都是先开辟内存空间，给这块内存写好数据，然后再把内存数据写回磁盘，所以才把写文件操作单独写一个函数。格式转换的过程都是内存数据操作，这样的话分工更加的分明

## 卡住的事 + 已尝试（没有就写"无"）

sws还有av_image_alloc都是要增加导库的

上下文是用来记入状态信息的，为了接下来的操作是必要的

## 今日时长: 3h

## 明日第一件事（只写一件）

-
