# D006 2026-09-23 周三

## 今日目标（昨天定的，1-3 条）

1.进行全量解码，完成计划任务

## 产出（必须至少有一条）

- 完成了全量解码，看到了解码的一部分具体过程

## 卡住的事 + 已尝试（没有就写"无"）

- 首先就是当前的样板代码还没有做到很好的释放资源，比如打开失败的时候直接就break，return了，没有做到很好的释放资源

- 还有就是先创建格式上下文，与文件连接绑定，创建decoder，创建解码器上下文，今天遇到的坑首先是忘了把decoder和解码器上下文绑定了，然后这件事需要在格式上下文和解码器上下文绑定之后。因为解码器是需要参数的，参数从哪来？参数从实际的文件获取，那谁与实际文件绑定？那就是AVFormatContext，所以我们要喂给解码器参数，解码器得通过解码器上下文做绑定获取参数，但是光创建一个解码器上下文是没参数的，不能直接绑定，要先和格式上下文做绑定，然后再解码器上下文和解码器绑定，不然整个过程是有问题的

## 今日时长: 2h

## 明日第一件事（只写一件）

-
