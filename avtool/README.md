# AVTOOL

FFmpeg学习工具 构建方式：

```
cmake -B build -G "Visual Studio 17 2022" -A x64
cmake --build build --config Release
./build/Release/avtool.exe
```

前置：FFmpeg已装在D:/FFmpeg/ffmpeg-master-latest-win64-gpl-shared

并已把路径添加到系统环境变量Path


